# jspace arc — figure inventory

Bijection between committed figures and their render scripts
(ARC_PROCESS step 3). No figures exist yet — the arc is at design stage;
rows are added here in the same commit as each figure + its render script.

| Figure | Render script | Data inputs | Status |
|--------|--------------|-------------|--------|
