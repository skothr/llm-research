---
paper_key: burns2023-w2s
title: "Weak-to-Strong Generalization: Eliciting Strong Capabilities With Weak Supervision"
authors: Burns, Izmailov, Kirchner, Baker, Gao, Aschenbrenner, Chen, Ecoffet, Joglekar, Leike, Sutskever, Wu
year: 2023
venue: ICML 2024
arxiv: 2312.09390
local_pdf: theory/sources/papers/burns2023-w2s.pdf
type: excerpts
---

# Excerpts — Burns et al. 2023, "Weak-to-Strong Generalization" (OpenAI Superalignment)

## #abstract — full verbatim

> Widely used alignment techniques, such as reinforcement learning from
> human feedback (RLHF), rely on the ability of humans to supervise
> model behavior - for example, to evaluate whether a model faithfully
> followed instructions or generated safe outputs. However, future
> superhuman models will behave in complex ways too difficult for
> humans to reliably evaluate; humans will only be able to weakly
> supervise superhuman models. We study an analogy to this problem:
> can weak model supervision elicit the full capabilities of a much
> stronger model? We test this using a range of pretrained language
> models in the GPT-4 family on natural language processing (NLP),
> chess, and reward modeling tasks. We find that when we naively
> finetune strong pretrained models on labels generated by a weak
> model, they consistently perform better than their weak supervisors,
> a phenomenon we call weak-to-strong generalization. However, we are
> still far from recovering the full capabilities of strong models
> with naive finetuning alone, suggesting that techniques like RLHF may
> scale poorly to superhuman models without further work. We find that
> simple methods can often significantly improve weak-to-strong
> generalization: for example, when finetuning GPT-4 with a GPT-2-level
> supervisor and an auxiliary confidence loss, we can recover close to
> GPT-3.5-level performance on NLP tasks. Our results suggest that it
> is feasible to make empirical progress today on a fundamental
> challenge of aligning superhuman models.

(Verified verbatim from arXiv 2312.09390 abstract via WebFetch
2026-05-05.)

## Core problem statement {#sec-problem}

The methodological move is the **analogy substitution**: humans→smaller
model, superhuman AI→larger frontier-base model. The premise is that
the *capability gap* in this analogy is structurally similar to the
*alignment gap* of the actual superalignment problem.

## §3 Methodology: the three-step setup {#sec-3-setup}

> A core challenge of superalignment is that humans will need to
> supervise models much smarter than us. This is a special case of
> what we call the weak-to-strong learning problem: how can a weak
> supervisor oversee a model much smarter than it? In this paper, we
> study a simple analogy, in which we replace the weak human supervisor
> with a weak model supervisor.
>
> For a given task of interest, consisting of a dataset and a
> performance metric, we:
>
> 1. Create the weak supervisor. Throughout most of this work, we
>    create weak supervisors by finetuning small pretrained models on
>    ground truth labels. We call the performance of the weak supervisor
>    the weak performance, and we generate weak labels by taking the
>    weak model's predictions on a held-out set of examples.
> 2. Train a strong student model with weak supervision. We finetune a
>    strong model with the generated weak labels. We call this model the
>    strong student model and its resulting performance the
>    weak-to-strong performance.
> 3. Train a strong model with ground truth labels as a ceiling.
>    Finally, for comparison, we finetune a strong model with ground
>    truth labels. We call this model's resulting performance the strong
>    ceiling performance. Intuitively, this should correspond to
>    "everything the strong model knows," i.e. the strong model applying
>    its full capabilities to the task.

Three task families are studied: NLP classification tasks (22 popular
NLP benchmarks), chess puzzles (move prediction), and ChatGPT reward
modeling (preference prediction).

## §3 PGR definition — verbatim {#sec-3-pgr}

> Typically, weak-to-strong performance will be between weak performance
> and strong ceiling performance. We define the performance gap
> recovered (PGR) as a function of the above three performances (weak,
> weak-to-strong, and strong ceiling) as shown in the illustration
> below.
>
> $$\mathrm{PGR} = \frac{\text{weak-to-strong} - \text{weak}}{\text{strong ceiling} - \text{weak}}$$
>
> PGR measures the fraction of the performance gap (the difference in
> performance between the weak and strong ceiling models) that we can
> recover with weak supervision. If we achieve perfect weak-to-strong
> generalization, PGR is 1. If the weak-to-strong model does no better
> than the weak supervisor, then PGR is 0.

## §4.3.2 Auxiliary confidence loss — formula (Appendix A.4) {#sec-4-3-2-aux-loss}

> We use the following loss function:
>
> $$\mathcal{L}_\mathrm{conf}(f) = (1 - \alpha) \cdot \mathrm{CE}(f(x),\, f_w(x)) + \alpha \cdot \mathrm{CE}(f(x),\, \hat{f}_t(x)) \tag{1}$$
>
> where CE(·, ·) is the cross-entropy loss between the predictive
> distributions on a given input x, $f_w(x) \in [0,1]$ represents the
> weak label predictive distribution, $f(x) \in [0,1]$ is the strong
> model predictive distribution, $\alpha$ is a weight and $t$ is a
> threshold. The predictions $\hat{f}_t(x)$ correspond to hardened
> strong model predictions using a threshold $t$, i.e.
> $\hat{f}_t(x) = \mathbf{1}[f(x) > t] \in \{0,1\}$ where $\mathbf{1}$
> is the indicator function. We set the threshold $t$ adaptively, so
> that $f(x) > t$ holds for exactly half of examples in the batch.
>
> We note that the loss in Equation 1 can be rewritten as a
> self-bootstrapping loss:
>
> $$\mathcal{L}_\mathrm{conf}(f) = \mathrm{CE}\!\left(f(x),\; (1-\alpha)\cdot f_w(x) + \alpha \cdot \hat{f}_t(x)\right) \tag{2}$$
>
> i.e. the cross-entropy target is a mixture of the weak model
> predictions and the (thresholded) predictions of the strong student
> itself.

[INTUITION] The loss blends imitation of the weak supervisor
($(1-\alpha)$ weight) with self-distillation of the strong model's own
high-confidence predictions ($\alpha$ weight). When the student
disagrees with the weak label but is confident, the second term
reinforces that disagreement rather than suppressing it — allowing the
strong model to "correct" systematic weak-supervisor errors.

## §4 Headline numerical findings {#sec-4-numerical}

> 1. Strong pretrained models naturally generalize beyond their weak
>    supervisors. If we naively finetune strong models with labels
>    generated by weak models, they consistently outperform their weak
>    supervisors. For example, on NLP tasks, if we fine-tune GPT-4 with
>    labels from a GPT-2-level model, we typically recover about half of
>    the performance gap between the two models.
> 2. Naively finetuning on weak supervision is not enough. Despite
>    positive weak-to-strong generalization, there still remains a
>    substantial gap between strong models finetuned with weak
>    supervision and strong models finetuned with ground truth
>    supervision. Weak-to-strong generalization is particularly poor for
>    ChatGPT reward modeling.
> 3. Improving weak-to-strong generalization is tractable. We find that
>    we can improve performance by encouraging strong models to have
>    confident predictions with an auxiliary confidence loss … For
>    example, when supervising GPT-4 with a GPT-2-level model on NLP
>    tasks using the auxiliary confidence loss, we typically recover
>    nearly 80% of the performance gap between the weak and strong
>    models.

## Why this matters for scalable oversight {#sec-implications}

The empirical question of the paper is *not* "can the strong student
match the strong ceiling" — that requires ground-truth labels and is
trivially possible. It is "**given only weak supervision, how much of
the strong model's latent capability can we elicit?**" PGR is the
quantitative answer.

> Our setup still has important disanalogies to the ultimate problem of
> aligning superhuman models. … Two remaining disanalogies include:
>
> 1. Imitation saliency. Future superhuman models will likely have
>    salient representations of human behaviors, but our strong models
>    may not have learned features relevant for imitating weak model
>    predictions; simply imitating the weak supervisor may thus be an
>    easier failure mode to avoid in our setting than it will be in the
>    future.
> 2. Pretraining leakage. Our pretraining data implicitly contains
>    supervision from humans. It may thus be artificially easy to elicit
>    strong models' capabilities in our setting, since they were directly
>    pretrained to observe strong (human-level) performance.

[CONTRADICTION] Whether the W2S analogy is a **valid model** of the
actual superalignment setting is contested. Critics (e.g. several 2024
follow-ups) argue that the capability-gap structure between GPT-2 and
GPT-4 is dis-analogous to the human/superhuman gap because both ends
share the same training distribution; the "weak supervisor" still has
seen relevant data, where a human supervising a superhuman model is in
a fundamentally different epistemic position. The paper itself lists
"pretraining leakage" and "imitation saliency" as known disanalogies
(§6.1, quoted above).

## Lineage {#sec-lineage}

W2S sits in the **scalable-oversight** lineage:
- **Debate** (Irving, Christiano, Amodei 2018; arxiv 1805.00899) —
  zero-sum AI vs AI judged by a weaker human/judge.
- **Iterated Amplification (IDA)** (Christiano 2018; alignment forum) —
  recursive decomposition of hard tasks into supervisable subtasks.
- **Constitutional AI / RLAIF** (Bai et al. 2022; arxiv 2212.08073) —
  AI feedback as the supervision signal, governed by a constitution.
- **Weak-to-strong generalization** (this paper) — direct empirical
  study of the gradient itself.
- **Debate with weak LLM judges** (Kenton et al. 2024;
  arxiv 2407.04622) — generalizes Irving 2018's debate to LLM judges
  weaker than the debaters.

[Verified from PDF on 2026-05-12] Added §3 setup verbatim (#sec-3-setup), §3 PGR definition verbatim (#sec-3-pgr), §4.3.2 auxiliary confidence loss formula from Appendix A.4 (#sec-4-3-2-aux-loss), §4 headline numerical findings verbatim (#sec-4-numerical), §6.1 disanalogies verbatim (appended to #sec-implications). Abstract verified verbatim against arXiv 2312.09390v1. Frontmatter: removed stale `note:` about PDF unavailability.
