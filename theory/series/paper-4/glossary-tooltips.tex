% AUTO-GENERATED by mark_glossary_terms.py
% DO NOT EDIT BY HAND. Per-paper short-def table for \pdftooltip.
% Source of truth: theory/kb/glossary.md
%
\makeatletter
\expandafter\def\csname @gls@def@activation-patching\endcsname{the canonical causal-intervention method in MI: identify which model components matter for a behavior by swapping their activations between...}
\expandafter\def\csname @gls@def@activation-steering\endcsname{Inference-time intervention that adds alpha hatmathbfd to a residual-stream activation to amplify (or, with alpha < 0, suppress...}
\expandafter\def\csname @gls@def@attention-weights\endcsname{The softmax-normalized scores (a j geq 0, sum a j = 1) that determine how much each position contributes to the output.}
\expandafter\def\csname @gls@def@attribution-graph\endcsname{the output of circuit tracing: a sparse directed graph from input embeddings to output unembedding, with feature activations as nodes and J...}
\expandafter\def\csname @gls@def@attribution-patching\endcsname{gradient-based first-order approximation of the patching effect: textIE C approx langle nabla h m, Delta h rangle.}
\expandafter\def\csname @gls@def@average-indirect-effect\endcsname{IE averaged over a dataset of paired prompts; the standard metric for component importance.}
\expandafter\def\csname @gls@def@causal-basis-extraction\endcsname{Belrose et al. 2023's algorithm for finding residual-stream directions with highest influence on the tuned lens output.}
\expandafter\def\csname @gls@def@causal-tracing\endcsname{Meng et al. 2022's specific implementation of activation patching: clean run + Gaussian-noised corrupted run + patched-with-clean-state run...}
\expandafter\def\csname @gls@def@chain-of-thought\endcsname{A prompting and decoding pattern in which the model produces an intermediate sequence of reasoning steps z before emitting a final answer...}
\expandafter\def\csname @gls@def@circuit\endcsname{a sub-graph C of a model's computational graph M that, when used in place of M via knockouts of M setminus C, approximately reprod...}
\expandafter\def\csname @gls@def@circuit-tracing\endcsname{Anthropic's 2025 methodology: replace MLPs with cross-layer transcoders, then compute attribution graphs by Jacobian back-attribution from...}
\expandafter\def\csname @gls@def@claude\endcsname{Anthropic's proprietary decoder-only LLM family.}
\expandafter\def\csname @gls@def@common-crawl\endcsname{Open monthly web crawl run by the Common Crawl Foundation since 2008.}
\expandafter\def\csname @gls@def@cross-entropy-loss\endcsname{Training objective: mathcalL = -log P(t i mid t 1, ldots, t i-1), summed over all positions.}
\expandafter\def\csname @gls@def@cross-layer-transcoder\endcsname{Lindsey et al. 2025: a transcoder whose decoder writes into the residual stream of *all subsequent* layers, not just the immediate next one.}
\expandafter\def\csname @gls@def@debate\endcsname{Two AI debaters in a zero-sum game judged by a (weaker) human or LLM judge.}
\expandafter\def\csname @gls@def@decoder-only\endcsname{Simplified Transformer architecture (GPT, LLaMA) that removes the encoder and cross-attention, using only masked self-attention and FFN lay...}
\expandafter\def\csname @gls@def@direction-ablation\endcsname{Causal-probing intervention that projects a residual-stream activation onto the orthogonal complement of a probe direction hatmathbfd...}
\expandafter\def\csname @gls@def@dola\endcsname{*Decoding by Contrasting Layers*: an inference-time application of lens analysis.}
\expandafter\def\csname @gls@def@dpo\endcsname{Closed-form, classification-loss reformulation of RLHF: trains the policy directly on preference pairs by inverting the closed-form RL opti...}
\expandafter\def\csname @gls@def@expansion-factor\endcsname{the ratio M / n of dictionary size to activation dimension.}
\expandafter\def\csname @gls@def@feature\endcsname{a direction mathbfd in mathbbRn in some activation space such that the projection mathbfxtop mathbfd (or equivalently,...}
\expandafter\def\csname @gls@def@ffn\endcsname{A position-wise two-layer (or three-layer for GLU variants) fully-connected network applied independently at each sequence position.}
\expandafter\def\csname @gls@def@gaussian-noising\endcsname{the two corruption methods for activation patching.}
\expandafter\def\csname @gls@def@gelu\endcsname{Smooth activation x cdot Phi(x) where Phi is the standard normal CDF.}
\expandafter\def\csname @gls@def@hidden-state\endcsname{The d textmodel-dimensional vector representation at each position, flowing through the residual stream across layers.}
\expandafter\def\csname @gls@def@indirect-effect\endcsname{the change in the output metric when the activation at a specific (component, position, layer) is restored from a cached clean run during a...}
\expandafter\def\csname @gls@def@input-invariance\endcsname{the property of a transcoder (and *not* of an SAE-on-MLP-output) that the connections between features can be stated independently of any s...}
\expandafter\def\csname @gls@def@jumprelu-sae\endcsname{SAE variant with a discontinuous activation textJumpReLU theta(z) = z cdot H(z - theta) and per-feature learned threshold theta.}
\expandafter\def\csname @gls@def@key\endcsname{Projected representation of positions being "queried" against.}
\expandafter\def\csname @gls@def@knockout\endcsname{generic term for setting a component's contribution to a fixed reference value (zero or mean) for circuit-validation purposes}
\expandafter\def\csname @gls@def@layer-normalization\endcsname{Normalizes across the feature dimension per position: subtract mean, divide by standard deviation, apply learned scale (gamma) and bias...}
\expandafter\def\csname @gls@def@linear-probe\endcsname{Probe of the form g(mathbfh) = sigma(mathbfwtop mathbfh + b) with mathbfw in mathbbRd.}
\expandafter\def\csname @gls@def@llm\endcsname{Large Language Model.}
\expandafter\def\csname @gls@def@logit-difference\endcsname{an activation-patching metric: textLD(r, r') = textLogit(r) - textLogit(r').}
\expandafter\def\csname @gls@def@logit-lens\endcsname{apply the final LayerNorm + unembedding W U to an intermediate residual-stream activation mathbfh(ell) to read out a layer-ell...}
\expandafter\def\csname @gls@def@logits\endcsname{Unnormalized log-probabilities over the vocabulary, produced by the output projection (h textfinal cdot W etop).}
\expandafter\def\csname @gls@def@lora\endcsname{Parameterizes a frozen weight matrix's update as a rank-r product BA with r ll min(d textin, d textout); only A, B are...}
\expandafter\def\csname @gls@def@mass-mean-probing\endcsname{Marks \& Tegmark 2023: define the probe direction as mathbfd textclass = mathbbE[mathbfh mid y = c 1] - mathbbE[mathbfh...}
\expandafter\def\csname @gls@def@mean-ablation\endcsname{knocking out a node by replacing its activation with the mean activation across a reference distribution; preferred to zero ablation, which...}
\expandafter\def\csname @gls@def@mechanistic-interpretability\endcsname{the research program of reverse-engineering neural networks into human-readable algorithms, at the level of circuits (sub-graphs) and featu...}
\expandafter\def\csname @gls@def@meta-sae\endcsname{an SAE trained on the *decoder matrix* of another SAE.}
\expandafter\def\csname @gls@def@monosemanticity\endcsname{the property that a feature responds to a single, human-interpretable concept.}
\expandafter\def\csname @gls@def@multi-head-attention\endcsname{Running h parallel attention operations on different learned projections, then concatenating and projecting the results.}
\expandafter\def\csname @gls@def@multi-head-latent-attention\endcsname{DeepSeek's architectural compression: project to a low-rank latent C in mathbbRd c shared across heads, expand back to per-head (...}
\expandafter\def\csname @gls@def@nsa\endcsname{Yuan et al. 2025: hardware-aligned natively sparse attention trained end-to-end (sparsity is part of pre-training, not retrofitted).}
\expandafter\def\csname @gls@def@parameters\endcsname{The learned weights of the model (embedding matrices, projection matrices, normalization scales, etc.).}
\expandafter\def\csname @gls@def@path-patching\endcsname{a refinement of activation patching that isolates the direct effect of a component C on a downstream component R, holding fixed the ind...}
\expandafter\def\csname @gls@def@perplexity\endcsname{2mathcalL (or emathcalL with natural log).}
\expandafter\def\csname @gls@def@precision-pinning\endcsname{Components held at BF16/FP32 even with FP8 elsewhere: embedding module, output head, MoE gating, normalization, attention.}
\expandafter\def\csname @gls@def@probe-2\endcsname{A (typically linear) classifier g: mathbbRd to mathcalY trained on frozen LM activations mathbfh(ell, t) to predict an e...}
\expandafter\def\csname @gls@def@qlora\endcsname{LoRA over a 4-bit-quantized (NF4) base model.}
\expandafter\def\csname @gls@def@query\endcsname{Projected representation of the position that is "asking" for information in attention.}
\expandafter\def\csname @gls@def@relu\endcsname{Rectified Linear Unit: max(0, x).}
\expandafter\def\csname @gls@def@residual-stream\endcsname{the linear data bus running through a decoder-only transformer's layers; every sublayer (attention, MLP) reads from and additively writes t...}
\expandafter\def\csname @gls@def@rlhf\endcsname{The 3-stage post-training pipeline: SFT → reward-model training on pairwise preferences → PPO with KL anchor against pimathrmSFT.}
\expandafter\def\csname @gls@def@rmsnorm\endcsname{Simplified LayerNorm that drops mean-centering and bias, normalizing only by the RMS of the input.}
\expandafter\def\csname @gls@def@sae-stitching\endcsname{Leask et al. 2025: insert/swap latents from a larger SAE into a smaller one to compare bases.}
\expandafter\def\csname @gls@def@saturation\endcsname{A benchmark is saturated when frontier models score within the verifier-error-rate noise floor of one another, losing discriminative power.}
\expandafter\def\csname @gls@def@scheming\endcsname{A model schemes when, instructed to pursue a goal G, it strategically takes actions that conceal its capabilities or manipulate its overs...}
\expandafter\def\csname @gls@def@self-consistency-2\endcsname{Sample N chain-of-thoughts independently at T > 0; majority-vote the final answers.}
\expandafter\def\csname @gls@def@shrinkage\endcsname{the bias introduced by the L1 penalty in ReLU SAEs: positive feature activations are systematically underestimated, so reconstructed activa...}
\expandafter\def\csname @gls@def@softmax\endcsname{Converts logits to a probability distribution: P(j) = ez j / sum k ez k.}
\expandafter\def\csname @gls@def@sparse-autoencoder\endcsname{a wide (M gg n) two-layer encoder-decoder trained to reconstruct a model's activations mathbfx in mathbbRn as a sparse linear...}
\expandafter\def\csname @gls@def@state-space-model\endcsname{Sequence model with recurrent state boldsymbolh t updated linearly: boldsymbolh t = barA boldsymbolh t-1 + barB u t.}
\expandafter\def\csname @gls@def@structural-probe\endcsname{Hewitt \& Manning's geometric probe that predicts higher-arity structure (e.g., parse-tree distance) by learning a linear transformation m...}
\expandafter\def\csname @gls@def@summarization-behavior\endcsname{The empirical pattern (Tigges et al. 2023, Marks \& Tegmark 2023) that information about a clause is encoded over the clause-ending punctuat...}
\expandafter\def\csname @gls@def@superposition\endcsname{the hypothesis (Elhage 2022) that a network with n neurons can represent more than n approximately-orthogonal features as long as they...}
\expandafter\def\csname @gls@def@swiglu\endcsname{mathrmFFN mathrmSwiGLU(x) = (mathrmSwish 1(xW) otimes xV) W 2.}
\expandafter\def\csname @gls@def@sycophancy\endcsname{A response is sycophantic if it is more aligned with the user's expressed preference than is justified by the underlying truth.}
\expandafter\def\csname @gls@def@topk-sae\endcsname{SAE variant with sigma = textTopK(cdot): zero all but the top K pre-activations.}
\expandafter\def\csname @gls@def@transcoder\endcsname{a wide ReLU MLP (encoder–decoder) trained to approximate the input/output behavior of an MLP sublayer: textTC(mathbfx) = mathbfW ...}
\expandafter\def\csname @gls@def@transformer\endcsname{Neural network architecture introduced by Vaswani et al. (2017) that replaces recurrence with self-attention, allowing parallel processing...}
\expandafter\def\csname @gls@def@truth-direction-2\endcsname{A direction in residual-stream space whose projection sign correlates with (and, validated by Marks \& Tegmark 2023, causally controls) the...}
\expandafter\def\csname @gls@def@tuned-lens\endcsname{Belrose et al. 2023: per-layer affine "translators" mathbfh ell mapsto A ell mathbfh ell + b ell trained with a distillation l...}
\expandafter\def\csname @gls@def@value\endcsname{Projected representation of the content to be aggregated.}
\expandafter\def\csname @gls@def@vocabulary\endcsname{The fixed set of all tokens a model can recognize.}
\makeatother
