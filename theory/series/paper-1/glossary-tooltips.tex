% AUTO-GENERATED by mark_glossary_terms.py
% DO NOT EDIT BY HAND. Per-paper short-def table for \pdftooltip.
% Source of truth: theory/kb/glossary.md
%
\makeatletter
\expandafter\def\csname @gls@def@alibi\endcsname{Attention with Linear Biases.}
\expandafter\def\csname @gls@def@attention-weights\endcsname{The softmax-normalized scores (a j geq 0, sum a j = 1) that determine how much each position contributes to the output.}
\expandafter\def\csname @gls@def@autoregressive\endcsname{Generating tokens one at a time, left to right, where each prediction depends only on previous tokens.}
\expandafter\def\csname @gls@def@awq\endcsname{Lin et al. 2023.}
\expandafter\def\csname @gls@def@bitlinear\endcsname{BitNet's drop-in replacement for `nn.Linear`: weights quantized to -1,0,+1 via absmean, activations to INT8 via absmax, SubLN replaci...}
\expandafter\def\csname @gls@def@bpe\endcsname{Subword tokenization algorithm that iteratively merges the most frequent adjacent character pairs to build a vocabulary.}
\expandafter\def\csname @gls@def@chain-of-thought\endcsname{A prompting and decoding pattern in which the model produces an intermediate sequence of reasoning steps z before emitting a final answer...}
\expandafter\def\csname @gls@def@circuit\endcsname{a sub-graph C of a model's computational graph M that, when used in place of M via knockouts of M setminus C, approximately reprod...}
\expandafter\def\csname @gls@def@claude\endcsname{Anthropic's proprietary decoder-only LLM family.}
\expandafter\def\csname @gls@def@compressed-fsm-2\endcsname{FSM transformation that merges chains of singular-transition edges into single edges, allowing multi-token decoding when the grammar determ...}
\expandafter\def\csname @gls@def@constrained-decoding-2\endcsname{Generation procedure that, at each step, masks the logits to allow only tokens consistent with a target grammar (regex, JSON schema, GBNF,...}
\expandafter\def\csname @gls@def@context-extension\endcsname{Sub-stage of late-stage pre-training that increases trained sequence length (e.g., 4 K → 32 K → 128 K) by adjusting RoPE scaling and contin...}
\expandafter\def\csname @gls@def@context-independent-context-dependent-vocabulary-split\endcsname{XGrammar's core trick: precompute (offline) the mask for tokens whose grammar-allowed-or-not status depends only on the grammar state (~99\%...}
\expandafter\def\csname @gls@def@cross-attention\endcsname{Attention where Q comes from the decoder and K, V come from the encoder output.}
\expandafter\def\csname @gls@def@data-mixing-law\endcsname{L i(mathbfr) = c i + k i exp(sum j t ij r j).}
\expandafter\def\csname @gls@def@decoder-only\endcsname{Simplified Transformer architecture (GPT, LLaMA) that removes the encoder and cross-attention, using only masked self-attention and FFN lay...}
\expandafter\def\csname @gls@def@encoder-decoder\endcsname{The original Transformer layout: an encoder processes the full input bidirectionally, a decoder generates output autoregressively, with cro...}
\expandafter\def\csname @gls@def@feature\endcsname{a direction mathbfd in mathbbRn in some activation space such that the projection mathbfxtop mathbfd (or equivalently,...}
\expandafter\def\csname @gls@def@ffn\endcsname{A position-wise two-layer (or three-layer for GLU variants) fully-connected network applied independently at each sequence position.}
\expandafter\def\csname @gls@def@flashattention\endcsname{Exact attention computed with re-organized memory access: tile K, V into SRAM-resident blocks and use **online softmax** to combine block...}
\expandafter\def\csname @gls@def@fp8\endcsname{IEEE-style 8-bit floating-point.}
\expandafter\def\csname @gls@def@gbnf\endcsname{llama.cpp's grammar specification format.}
\expandafter\def\csname @gls@def@geglu\endcsname{GLU with GELU activation: (mathrmGELU(xW) otimes xV) W 2.}
\expandafter\def\csname @gls@def@gelu\endcsname{Smooth activation x cdot Phi(x) where Phi is the standard normal CDF.}
\expandafter\def\csname @gls@def@global-batch-load-balancing\endcsname{Qwen3's strategy: apply load-balancing loss across the entire training mini-batch (cluster-wide), not within microbatches.}
\expandafter\def\csname @gls@def@glu\endcsname{mathrmGLU(x, W, V) = sigma(xW) otimes (xV), the elementwise product of a sigmoid-gated and an identity-gated linear projection.}
\expandafter\def\csname @gls@def@gptq\endcsname{Frantar et al. 2022.}
\expandafter\def\csname @gls@def@gqa\endcsname{Interpolation between MHA and MQA: query heads are partitioned into g groups, each group shares one WK, WV.}
\expandafter\def\csname @gls@def@hbm\endcsname{The large, slow memory tier on a GPU (~40–80 GB on A100, 1.5–2.0 TB/s).}
\expandafter\def\csname @gls@def@hidden-state\endcsname{The d textmodel-dimensional vector representation at each position, flowing through the residual stream across layers.}
\expandafter\def\csname @gls@def@hippo\endcsname{"High-order Polynomial Projection Operator." Specific structured choice of A matrix that optimally compresses input history under polynom...}
\expandafter\def\csname @gls@def@interleaved-mrope\endcsname{Qwen3-VL refinement of M-RoPE that interleaves the temporal/height/width axes per dimension-pair instead of partitioning the channel range;...}
\expandafter\def\csname @gls@def@irope\endcsname{"interleaved RoPE." LLaMA 4's position scheme: alternates RoPE-equipped attention layers with NoPE attention layers (3:1 ratio) plus temper...}
\expandafter\def\csname @gls@def@key\endcsname{Projected representation of positions being "queried" against.}
\expandafter\def\csname @gls@def@kv-cache\endcsname{The per-layer cache of key/value tensors K 1:t, V 1:t produced during autoregressive decoding so each new token's attention is O(t)...}
\expandafter\def\csname @gls@def@layer-normalization\endcsname{Normalizes across the feature dimension per position: subtract mean, divide by standard deviation, apply learned scale (gamma) and bias...}
\expandafter\def\csname @gls@def@llm\endcsname{Large Language Model.}
\expandafter\def\csname @gls@def@logits\endcsname{Unnormalized log-probabilities over the vocabulary, produced by the output projection (h textfinal cdot W etop).}
\expandafter\def\csname @gls@def@lstm\endcsname{Long Short-Term Memory.}
\expandafter\def\csname @gls@def@m-rope\endcsname{Multi-axis RoPE for vision-language models.}
\expandafter\def\csname @gls@def@mha\endcsname{The Vaswani 2017 default: h independent heads, each with its own W iQ, W iK, W iV.}
\expandafter\def\csname @gls@def@mistral\endcsname{Open-weights decoder-only LLM family from Mistral AI (Jiang et al., 2023).}
\expandafter\def\csname @gls@def@mqa\endcsname{Variant where all heads share a single WK, WV (only WQ remains per-head).}
\expandafter\def\csname @gls@def@multi-head-attention\endcsname{Running h parallel attention operations on different learned projections, then concatenating and projecting the results.}
\expandafter\def\csname @gls@def@multi-head-latent-attention\endcsname{DeepSeek's architectural compression: project to a low-rank latent C in mathbbRd c shared across heads, expand back to per-head (...}
\expandafter\def\csname @gls@def@multi-token-prediction\endcsname{Training objective + architectural feature: in addition to the standard next-token head, the model has auxiliary heads predicting tokens at...}
\expandafter\def\csname @gls@def@native-sparse-attention\endcsname{Three-branch sparse attention (compress + select + sliding-window) trained natively (not retrofit).}
\expandafter\def\csname @gls@def@nope\endcsname{"No Position Encoding." Decoder-only Transformer trained without any explicit positional encoding; relies on the causal mask to recover pos...}
\expandafter\def\csname @gls@def@nsa\endcsname{Yuan et al. 2025: hardware-aligned natively sparse attention trained end-to-end (sparsity is part of pre-training, not retrofitted).}
\expandafter\def\csname @gls@def@nvfp4\endcsname{4-bit floating-point format (E2M1) with native hardware MXScale (32-element per-block scaling) on Blackwell-class GPUs.}
\expandafter\def\csname @gls@def@online-softmax\endcsname{Numerically stable streaming softmax: maintain running max m and normalizer ell across blocks; when a new block arrives, rescale the e...}
\expandafter\def\csname @gls@def@pagedattention\endcsname{vLLM's KV-cache layout: KV is stored in fixed-size **blocks** (typically 16 tokens), each sequence holds a per-layer **block table** mappin...}
\expandafter\def\csname @gls@def@palm\endcsname{Google's 540B-parameter decoder-only LLM (Chowdhery et al., 2022).}
\expandafter\def\csname @gls@def@parallel-scan\endcsname{GPU-efficient associative-scan algorithm used by Mamba to compute the input-dependent recurrence in O(N log N) during training while the...}
\expandafter\def\csname @gls@def@parameters\endcsname{The learned weights of the model (embedding matrices, projection matrices, normalization scales, etc.).}
\expandafter\def\csname @gls@def@perplexity\endcsname{2mathcalL (or emathcalL with natural log).}
\expandafter\def\csname @gls@def@position-interpolation\endcsname{Simplest RoPE extension: scale positions by s = L texttest / L texttrain.}
\expandafter\def\csname @gls@def@post-ln\endcsname{Original Transformer placement: normalize after the residual addition.}
\expandafter\def\csname @gls@def@pre-ln\endcsname{Modern placement: normalize before the sub-layer, then add residually.}
\expandafter\def\csname @gls@def@precision-pinning\endcsname{Components held at BF16/FP32 even with FP8 elsewhere: embedding module, output head, MoE gating, normalization, attention.}
\expandafter\def\csname @gls@def@query\endcsname{Projected representation of the position that is "asking" for information in attention.}
\expandafter\def\csname @gls@def@radixattention\endcsname{SGLang's cross-request KV reuse: a radix tree keyed on token-id sequences indexes which prefixes are still in cache, with LRU + reference-c...}
\expandafter\def\csname @gls@def@recall-bottleneck\endcsname{SSM weakness: fixed-size state boldsymbolh t cannot store every prior token's exact features, so exact-retrieval tasks (copy, NIAH) un...}
\expandafter\def\csname @gls@def@rejection-sampling\endcsname{Llama-3 SFT quality gate: sample N candidates from the model, score by reward model, keep top-k, SFT on those.}
\expandafter\def\csname @gls@def@relative-position-encoding\endcsname{Per-(query, key) distance bias added to attention logits; binned by relative distance.}
\expandafter\def\csname @gls@def@relu\endcsname{Rectified Linear Unit: max(0, x).}
\expandafter\def\csname @gls@def@residual-connection\endcsname{Adding the sub-layer input directly to its output (textoutput = x + textSubLayer(x)).}
\expandafter\def\csname @gls@def@residual-stream\endcsname{the linear data bus running through a decoder-only transformer's layers; every sublayer (attention, MLP) reads from and additively writes t...}
\expandafter\def\csname @gls@def@rlhf\endcsname{The 3-stage post-training pipeline: SFT → reward-model training on pairwise preferences → PPO with KL anchor against pimathrmSFT.}
\expandafter\def\csname @gls@def@rmsnorm\endcsname{Simplified LayerNorm that drops mean-centering and bias, normalizing only by the RMS of the input.}
\expandafter\def\csname @gls@def@rnn\endcsname{Recurrent Neural Network.}
\expandafter\def\csname @gls@def@rope\endcsname{Positional encoding that rotates query and key vectors by position-dependent angles, making attention dot products depend on relative posit...}
\expandafter\def\csname @gls@def@s4\endcsname{Structured State Space sequence model with HiPPO-NPLR A, FFT-based convolutional training, recurrent inference.}
\expandafter\def\csname @gls@def@scaled-dot-product-attention\endcsname{Core attention operation: textsoftmax(QKtop / sqrtd k) V.}
\expandafter\def\csname @gls@def@selective-ssm\endcsname{Mamba's contribution: input-dependent barB, C, Delta (parameters produced by linear projection of current input), enabling content-aw...}
\expandafter\def\csname @gls@def@self-attention\endcsname{Mechanism where each position in a sequence computes a weighted sum over all positions, with weights determined by learned query-key simila...}
\expandafter\def\csname @gls@def@sentencepiece\endcsname{A tokenizer that operates on raw byte sequences rather than Unicode characters, used by LLaMA.}
\expandafter\def\csname @gls@def@sliding-window-attention\endcsname{Each position attends only to the previous w tokens; compute O(N w d) vs O(N2 d).}
\expandafter\def\csname @gls@def@softmax\endcsname{Converts logits to a probability distribution: P(j) = ez j / sum k ez k.}
\expandafter\def\csname @gls@def@speedup-theorem\endcsname{mathbbE[texttokens per cycle] = (1-alphagamma+1)/(1-alpha) (Eq.1, capped geometric).}
\expandafter\def\csname @gls@def@state-space-duality\endcsname{Mamba-2's framing: selective SSMs and a structurally-restricted form of attention compute the same function.}
\expandafter\def\csname @gls@def@state-space-model\endcsname{Sequence model with recurrent state boldsymbolh t updated linearly: boldsymbolh t = barA boldsymbolh t-1 + barB u t.}
\expandafter\def\csname @gls@def@swiglu\endcsname{mathrmFFN mathrmSwiGLU(x) = (mathrmSwish 1(xW) otimes xV) W 2.}
\expandafter\def\csname @gls@def@t5\endcsname{Google's encoder-decoder Transformer (Raffel et al., 2019) that frames every NLP task as text-to-text.}
\expandafter\def\csname @gls@def@tiling\endcsname{Restructuring a computation to operate on sub-blocks that fit in fast memory.}
\expandafter\def\csname @gls@def@token-embedding\endcsname{A learned lookup table (W e in mathbbRV times d textmodel) mapping each token index to a dense vector.}
\expandafter\def\csname @gls@def@tokenization\endcsname{Converting raw text into a sequence of discrete integer token IDs that a model can process.}
\expandafter\def\csname @gls@def@transformer\endcsname{Neural network architecture introduced by Vaswani et al. (2017) that replaces recurrence with self-attention, allowing parallel processing...}
\expandafter\def\csname @gls@def@tree-attention\endcsname{A draft tree of token candidates verified in one target forward pass via a custom attention mask that lets every node attend only to its an...}
\expandafter\def\csname @gls@def@typical-acceptance\endcsname{Medusa's quality–throughput knob: accept any draft token whose probability under the target exceeds min(epsilon, delta cdot exp(-H(p)...}
\expandafter\def\csname @gls@def@uptraining\endcsname{The Ainslie 2023 recipe for converting an existing MHA checkpoint to GQA/MQA: mean-pool the WK, WV matrices within each group, then con...}
\expandafter\def\csname @gls@def@value\endcsname{Projected representation of the content to be aggregated.}
\expandafter\def\csname @gls@def@vocabulary\endcsname{The fixed set of all tokens a model can recognize.}
\expandafter\def\csname @gls@def@warmup\endcsname{Gradually increasing the learning rate from zero during early training steps.}
\expandafter\def\csname @gls@def@weight-tying\endcsname{Sharing the same weight matrix W e for both input embeddings and the output projection to logits, halving vocabulary-related parameters.}
\expandafter\def\csname @gls@def@yarn\endcsname{"Yet another RoPE extensioN method." Combines NTK-aware scaling with a per-frequency interpolation schedule (no scaling on short-wavelength...}
\makeatother
