% AUTO-GENERATED by mark_glossary_terms.py
% DO NOT EDIT BY HAND. Per-paper short-def table for \pdftooltip.
% Source of truth: theory/kb/glossary.md
%
\makeatletter
\expandafter\def\csname @gls@def@ablation-model\endcsname{Small reference model (1.71 B params, ~28 B tokens) trained on a candidate corpus to score it.}
\expandafter\def\csname @gls@def@adamw\endcsname{Adam with decoupled weight decay.}
\expandafter\def\csname @gls@def@aha-moment\endcsname{A spike in the model's use of reflection words like "wait" during training, taken as evidence of self-reorganization of reasoning structure...}
\expandafter\def\csname @gls@def@alignment-tax\endcsname{The performance regression on standard NLP benchmarks observed after RLHF.}
\expandafter\def\csname @gls@def@asynctp\endcsname{Fractionalizes TP matmuls into chunks and overlaps each chunk's communication with the previous chunk's compute via intra-node `SymmetricMe...}
\expandafter\def\csname @gls@def@autoregressive\endcsname{Generating tokens one at a time, left to right, where each prediction depends only on previous tokens.}
\expandafter\def\csname @gls@def@auxiliary-loss-free-load-balancing\endcsname{DeepSeek-V3's strategy: add a per-expert bias to router logits (not to gating weights), update bias online based on observed load.}
\expandafter\def\csname @gls@def@bitlinear\endcsname{BitNet's drop-in replacement for `nn.Linear`: weights quantized to -1,0,+1 via absmean, activations to INT8 via absmax, SubLN replaci...}
\expandafter\def\csname @gls@def@bpe\endcsname{Subword tokenization algorithm that iteratively merges the most frequent adjacent character pairs to build a vocabulary.}
\expandafter\def\csname @gls@def@capability-ceiling\endcsname{The empirical finding that RLVR improves mathrmpass@1 on problems the base model could already solve at mathrmpass@k for some k...}
\expandafter\def\csname @gls@def@chain-of-thought\endcsname{A prompting and decoding pattern in which the model produces an intermediate sequence of reasoning steps z before emitting a final answer...}
\expandafter\def\csname @gls@def@chinchilla-parametric-loss\endcsname{The functional form hatL(N, D) = E + A/Nalpha + B/Dbeta with five fitted constants.}
\expandafter\def\csname @gls@def@chinchilla-trap\endcsname{Community term for the practical problem that a Chinchilla-compute-optimal model is often too large to *serve* economically.}
\expandafter\def\csname @gls@def@claude\endcsname{Anthropic's proprietary decoder-only LLM family.}
\expandafter\def\csname @gls@def@clip-higher\endcsname{DAPO's asymmetric PPO clip [1-epsilon textlow, 1+epsilon texthigh] with epsilon texthigh > epsilon textlow.}
\expandafter\def\csname @gls@def@cold-start-sft-2\endcsname{A small-scale (sim 104 examples) SFT pass on long-CoT exemplars, run before RL to fix readability problems of pure-RL outputs (R1-Zero).}
\expandafter\def\csname @gls@def@common-crawl\endcsname{Open monthly web crawl run by the Common Crawl Foundation since 2008.}
\expandafter\def\csname @gls@def@constitution\endcsname{A list of principles ("the assistant should not be racist," etc.) used by the feedback model to evaluate / critique responses in CAI.}
\expandafter\def\csname @gls@def@constitutional-ai\endcsname{Anthropic 2022 framework for training harmless assistants without human harm labels.}
\expandafter\def\csname @gls@def@context-extension\endcsname{Sub-stage of late-stage pre-training that increases trained sequence length (e.g., 4 K → 32 K → 128 K) by adjusting RoPE scaling and contin...}
\expandafter\def\csname @gls@def@context-parallel\endcsname{Token-position sharding of the sequence dimension.}
\expandafter\def\csname @gls@def@cosine-schedule\endcsname{Learning-rate decay eta t = eta min + tfrac12(eta max-eta min)(1 + cos(pi t / T)), peaking after warmup and decaying...}
\expandafter\def\csname @gls@def@cross-entropy-loss\endcsname{Training objective: mathcalL = -log P(t i mid t 1, ldots, t i-1), summed over all positions.}
\expandafter\def\csname @gls@def@custom-heuristic-filters\endcsname{Three quantile-derived rules: lines-ending-with-terminal-punctuation leq 0.12, char-duplication geq 0.01, short-line-fraction geq 0...}
\expandafter\def\csname @gls@def@dapo-2\endcsname{ByteDance's GRPO variant.}
\expandafter\def\csname @gls@def@dare\endcsname{Bernoulli-drop 1-p of task-vector}
\expandafter\def\csname @gls@def@data-mixing-law\endcsname{L i(mathbfr) = c i + k i exp(sum j t ij r j).}
\expandafter\def\csname @gls@def@data-parallel\endcsname{Same parameters on each worker; each worker processes a shard of the global batch; gradients all-reduced before optimizer step.}
\expandafter\def\csname @gls@def@debate\endcsname{Two AI debaters in a zero-sum game judged}
\expandafter\def\csname @gls@def@decoder-only\endcsname{Simplified Transformer architecture (GPT, LLaMA) that removes the encoder and cross-attention, using only masked self-attention and FFN lay...}
\expandafter\def\csname @gls@def@dpo\endcsname{Closed-form, classification-loss reformulation of RLHF: trains the policy directly on preference pairs by inverting the closed-form RL opti...}
\expandafter\def\csname @gls@def@dtensor-devicemesh\endcsname{Distributed tensor abstraction; weights are stored as DTensor with a placement spec on a d times t times p times c times e device mes...}
\expandafter\def\csname @gls@def@dualpipe\endcsname{DeepSeek-V3's bidirectional PP.}
\expandafter\def\csname @gls@def@dynamic-sampling\endcsname{Reject groups where all G trajectories share the same reward, since the GRPO advantage is then zero and the gradient is wasted.}
\expandafter\def\csname @gls@def@expert-parallel\endcsname{MoE-specific sharding: each worker hosts a subset of the experts.}
\expandafter\def\csname @gls@def@feature\endcsname{a direction mathbfd in mathbbRn in some}
\expandafter\def\csname @gls@def@ffn\endcsname{A position-wise two-layer (or three-layer for GLU variants) fully-connected network applied independently at each sequence position.}
\expandafter\def\csname @gls@def@fineweb-edu\endcsname{1.3 T-token quality-classifier-filtered subset of FineWeb.}
\expandafter\def\csname @gls@def@flan\endcsname{Original instruction-tuning}
\expandafter\def\csname @gls@def@format-reward\endcsname{In RLVR, a small constant reward for trajectories that conform to a structural template (e.g., emit a `<think>...</think>` block).}
\expandafter\def\csname @gls@def@fp8\endcsname{IEEE-style 8-bit floating-point.}
\expandafter\def\csname @gls@def@fsdp\endcsname{"Fully Sharded Data Parallel." Native PyTorch ZeRO implementation.}
\expandafter\def\csname @gls@def@fsdp2\endcsname{Per-parameter `DTensor`-sharded successor to FSDP1.}
\expandafter\def\csname @gls@def@gqa\endcsname{Interpolation between MHA and MQA: query heads are partitioned into g groups, each group shares one WK, WV.}
\expandafter\def\csname @gls@def@group-normalized-advantage\endcsname{In GRPO, tildeA i = (R i - mathrmmean(R j))/mathrmstd(R j), broadcast to every token of trajectory o i.}
\expandafter\def\csname @gls@def@grpo\endcsname{Critic-free PPO variant: replaces the value model with a group-mean reward baseline.}
\expandafter\def\csname @gls@def@grpo-2\endcsname{Critic-free PPO variant (Shao 2024).}
\expandafter\def\csname @gls@def@hidden-state\endcsname{The d textmodel-dimensional vector representation at each position, flowing through the residual stream across layers.}
\expandafter\def\csname @gls@def@implicit-reward\endcsname{In DPO, the policy log-ratio hatr theta(x,y) = betalogpi theta(y|x)/pi mathrmref(y|x) that plays the role of a reward funct...}
\expandafter\def\csname @gls@def@ipo\endcsname{DPO variant replacing log-sigmoid loss with squared loss on the preference gap, addressing DPO's overconfidence pathology `[azar2023-ipo]`.}
\expandafter\def\csname @gls@def@key\endcsname{Projected representation of positions being "queried" against.}
\expandafter\def\csname @gls@def@kto\endcsname{Operates on single-rating data (x,y,mathrmlabelintextgood,textbad) instead of pairwise preferences.}
\expandafter\def\csname @gls@def@kv-cache\endcsname{The per-layer cache of key/value tensors K 1:t, V 1:t produced during autoregressive decoding so each new token's attention is O(t)...}
\expandafter\def\csname @gls@def@layer-normalization\endcsname{Normalizes across the feature dimension per position: subtract mean, divide by standard deviation, apply learned scale (gamma) and bias...}
\expandafter\def\csname @gls@def@linear-mode-connectivity-2\endcsname{The empirical property that}
\expandafter\def\csname @gls@def@lion\endcsname{"Evolved sign momentum." Lighter optimizer using only sign of momentum: mathbfw t leftarrow mathbfw t-1 - eta cdot textsign(...}
\expandafter\def\csname @gls@def@llm\endcsname{Large Language Model.}
\expandafter\def\csname @gls@def@logits\endcsname{Unnormalized log-probabilities over the vocabulary, produced by the output projection (h textfinal cdot W etop).}
\expandafter\def\csname @gls@def@lora\endcsname{Parameterizes a frozen weight}
\expandafter\def\csname @gls@def@loss-scaling\endcsname{Multiplying the loss by a scale factor S}
\expandafter\def\csname @gls@def@magpie\endcsname{Alignment data synthesis from scratch: prompt aligned LLMs with only their instruction-template prefix (no question), have them fabricate t...}
\expandafter\def\csname @gls@def@master-weights\endcsname{A high-precision (FP32) copy of the model}
\expandafter\def\csname @gls@def@mergekit\endcsname{Open-source toolkit for model merging supporting}
\expandafter\def\csname @gls@def@metap\endcsname{Meta's name for their cross-scale HP-transfer approach in the Llama 4 tech report `[meta-llama4]`.}
\expandafter\def\csname @gls@def@mfu\endcsname{Realized training throughput as a fraction of peak hardware FLOPS.}
\expandafter\def\csname @gls@def@minhash-deduplication\endcsname{Locality-sensitive hashing for near-duplicate detection: n-gram-shingle the document, sample H hash-of-min-hash signatures arranged as...}
\expandafter\def\csname @gls@def@mistral\endcsname{Open-weights decoder-only LLM family from Mistral AI (Jiang et al., 2023).}
\expandafter\def\csname @gls@def@mmlu\endcsname{57-subject 4-way MCQ benchmark introduced by Hendrycks et al. 2020 / ICLR 2021.}
\expandafter\def\csname @gls@def@model-merging\endcsname{Combining trained models via parameter-level operations (averaging, interpolation, sign-resolution, sparsification).}
\expandafter\def\csname @gls@def@multi-head-latent-attention\endcsname{DeepSeek's architectural compression: project to a low-rank latent C in mathbbRd c shared across heads, expand back to per-head (...}
\expandafter\def\csname @gls@def@muon\endcsname{"MomentUm Orthogonalized via Newton-Schulz." Replaces Adam's per-parameter rescaling with a *per-shape* matrix-orthogonalization on momentu...}
\expandafter\def\csname @gls@def@newton-schulz-iteration\endcsname{Iterative polynomial mathbfX k = amathbfX k-1 + b(mathbfX k-1mathbfX k-1top)mathbfX k-1 + c(mathbfX k-1math...}
\expandafter\def\csname @gls@def@nf4\endcsname{4-bit quantization with quantile-spaced}
\expandafter\def\csname @gls@def@non-embedding-parameters\endcsname{Parameter count excluding token-embedding and positional-embedding tables.}
\expandafter\def\csname @gls@def@non-evasion\endcsname{A model trained with}
\expandafter\def\csname @gls@def@nvfp4\endcsname{4-bit floating-point format (E2M1) with native}
\expandafter\def\csname @gls@def@orpo\endcsname{Combines SFT and preference losses in a single objective; no reference model, no SFT stage.}
\expandafter\def\csname @gls@def@palm\endcsname{Google's 540B-parameter decoder-only LLM (Chowdhery et al., 2022).}
\expandafter\def\csname @gls@def@parameters\endcsname{The learned weights of the model (embedding matrices, projection matrices, normalization scales, etc.).}
\expandafter\def\csname @gls@def@peft\endcsname{Family of fine-tuning methods that update only a small fraction of parameters: LoRA, DoRA, IA3, prefix-tuning.}
\expandafter\def\csname @gls@def@per-shape-rms-scaling\endcsname{Multiplier 0.2 cdot sqrtmax(A, B) applied to the orthogonalized update for an Atimes B weight matrix, equalizing update RMS acros...}
\expandafter\def\csname @gls@def@perplexity\endcsname{2mathcalL (or emathcalL with natural log).}
\expandafter\def\csname @gls@def@pipeline-parallel\endcsname{Stage-level layer split.}
\expandafter\def\csname @gls@def@ppo-ptx\endcsname{InstructGPT's RLHF objective with an added pre-training-mixture term: +gamma, mathbbE mathrmpretrain[logpi theta].}
\expandafter\def\csname @gls@def@precision-pinning\endcsname{Components held at BF16/FP32 even with FP8 elsewhere: embedding module, output head, MoE gating, normalization, attention.}
\expandafter\def\csname @gls@def@probe-2\endcsname{A (typically linear) classifier g: mathbbRd to mathcalY trained on frozen LM activations mathbfh(ell, t) to predict an e...}
\expandafter\def\csname @gls@def@qlora\endcsname{LoRA over a 4-bit-quantized (NF4) base model.}
\expandafter\def\csname @gls@def@query\endcsname{Projected representation of the position that is "asking" for information in attention.}
\expandafter\def\csname @gls@def@r-cai-2\endcsname{Inverts the constitution and applies}
\expandafter\def\csname @gls@def@r1-zero\endcsname{DeepSeek-R1-Zero, the version of R1 trained with **no SFT cold-start** — pure GRPO RL applied directly to the DeepSeek-V3 base.}
\expandafter\def\csname @gls@def@rejection-sampling\endcsname{Llama-3 SFT quality gate: sample N candidates from the model, score by reward model, keep top-k, SFT on those.}
\expandafter\def\csname @gls@def@rejection-sampling-sft\endcsname{Generate candidate solutions with the current policy, filter by verifier (and optional quality filter), SFT on the survivors.}
\expandafter\def\csname @gls@def@residual-stream\endcsname{the linear data bus running through a}
\expandafter\def\csname @gls@def@reward-hacking\endcsname{Policy exploits an artifact of the reward signal that does not correspond to true quality (e.g., verbosity, sycophancy, format-shortcuts).}
\expandafter\def\csname @gls@def@reward-model\endcsname{A neural network trained on Bradley–Terry preferences to score response quality.}
\expandafter\def\csname @gls@def@reward-overoptimization\endcsname{The gap between proxy reward r phi and gold reward grows as KL(pi theta|pi mathrmref) grows; the policy "hacks" the RM, scori...}
\expandafter\def\csname @gls@def@rl-cai-2\endcsname{Stage 2 of Constitutional AI: sample response pairs}
\expandafter\def\csname @gls@def@rlaif-3\endcsname{RLHF pipeline}
\expandafter\def\csname @gls@def@rlhf\endcsname{The 3-stage post-training pipeline: SFT → reward-model training on pairwise preferences → PPO with KL anchor against pimathrmSFT.}
\expandafter\def\csname @gls@def@rlvr-3\endcsname{RL post-training where the reward is computed mechanically from the emitted answer (regex match for math, unit-test execution for code, pro...}
\expandafter\def\csname @gls@def@rmsnorm\endcsname{Simplified LayerNorm that drops mean-centering and bias, normalizing only by the RMS of the input.}
\expandafter\def\csname @gls@def@rope\endcsname{Positional encoding that rotates query and key vectors by position-dependent angles, making attention dot products depend on relative posit...}
\expandafter\def\csname @gls@def@s4\endcsname{Structured State Space sequence model with HiPPO-NPLR A, FFT-based convolutional training, recurrent inference.}
\expandafter\def\csname @gls@def@scaling-law\endcsname{An empirical or theoretical functional relationship between a measure of model performance (typically cross-entropy loss) and one or more "...}
\expandafter\def\csname @gls@def@schatten-p-norm\endcsname{Matrix norm |mathbfM| p(s) = (sum i sigma ip)1/p on singular values.}
\expandafter\def\csname @gls@def@self-consistency-2\endcsname{Sample N chain-of-thoughts independently at T > 0; majority-vote the final answers.}
\expandafter\def\csname @gls@def@sequence-parallel\endcsname{Companion to TP: shards normalization and dropout layers along the sequence dimension to reduce activation memory.}
\expandafter\def\csname @gls@def@simpo\endcsname{Reference-free DPO variant using length-normalized average log-prob as implicit reward, plus a target reward margin gamma.}
\expandafter\def\csname @gls@def@sl-cai-2\endcsname{Stage 1 of Constitutional AI: sample a response from}
\expandafter\def\csname @gls@def@softmax\endcsname{Converts logits to a probability distribution: P(j) = ez j / sum k ez k.}
\expandafter\def\csname @gls@def@sophia\endcsname{"Second-order clipped stochastic optimization." Diagonal-Hessian preconditioning with clipping.}
\expandafter\def\csname @gls@def@spoonfeeding\endcsname{`[INTUITION]` Synthetic data presents *concept-then-application-then-explanation* as a self-contained unit, vs. organic web data which scat...}
\expandafter\def\csname @gls@def@swiglu\endcsname{mathrmFFN mathrmSwiGLU(x) = (mathrmSwish 1(xW) otimes xV) W 2.}
\expandafter\def\csname @gls@def@sycophancy\endcsname{A response is sycophantic if it is more aligned with}
\expandafter\def\csname @gls@def@t5\endcsname{Google's encoder-decoder Transformer (Raffel et al., 2019) that frames every NLP task as text-to-text.}
\expandafter\def\csname @gls@def@task-vector\endcsname{A direction in weight space defined as}
\expandafter\def\csname @gls@def@tensor-parallel\endcsname{Within-layer matmul split (column-parallel `Y = XA`, row-parallel `Z = YB`).}
\expandafter\def\csname @gls@def@test-time-compute\endcsname{The inference-side compute spent on a single problem instance to improve answer quality, beyond a single greedy forward pass.}
\expandafter\def\csname @gls@def@ties-merging\endcsname{Three-step model}
\expandafter\def\csname @gls@def@tokens-per-parameter-ratio\endcsname{D / N.}
\expandafter\def\csname @gls@def@trafilatura\endcsname{Python text-extraction library; extracts main article text from raw HTML.}
\expandafter\def\csname @gls@def@transformer\endcsname{Neural network architecture introduced by Vaswani et al. (2017) that replaces recurrence with self-attention, allowing parallel processing...}
\expandafter\def\csname @gls@def@value\endcsname{Projected representation of the content to be aggregated.}
\expandafter\def\csname @gls@def@vapo\endcsname{RL variant that re-introduces a learned value function on top of GRPO's design, with shared backbone and value-loss-clipping.}
\expandafter\def\csname @gls@def@vocabulary\endcsname{The fixed set of all tokens a model can recognize.}
\expandafter\def\csname @gls@def@warm\endcsname{Average parameters of K independently-trained RMs to reduce overoptimization and improve robustness.}
\expandafter\def\csname @gls@def@warmup\endcsname{Gradually increasing the learning rate from zero during early training steps.}
\expandafter\def\csname @gls@def@warp\endcsname{Alternates RL training with policy weight averaging; companion to WARM at the policy side.}
\expandafter\def\csname @gls@def@wsd-schedule\endcsname{Three-phase LR: linear warmup, then constant at eta max for the bulk of training, then short cooldown to ~0.1eta max.}
\expandafter\def\csname @gls@def@zero-1-zero-2-zero-3\endcsname{Optimizer-state / gradient / parameter sharding levels of Rajbhandari et al.'s Zero-Redundancy Optimizer.}
\expandafter\def\csname @gls@def@zerobubble\endcsname{PP schedule that splits backward into "backward-for-input" (B) and "backward-for-weights" (W); reorders W into the bubble.}
\makeatother
